const favoritesPageToggle = '.bt-favorites-mode-toggle';
const btResultsCount = '.bt-favorites-link__count';
const unflaggedFavoriteBtn = `.bt-favorite--unflagged`;
const flaggedFavoriteBtn   = `.bt-favorite--flagged`;
const offerContainer = '.bt-teaser';
const noFavorites = '.bt-no-results--favorites';
const changeFavoriteStatus = '.bt-teaser :nth-child(4)';
const filterBtn = '.bt-modal-toggle--open';
const cookieBanner = '.sqs-cookie-banner-v2-accept';

export default {
    favoritesPageToggle,
    btResultsCount,
    unflaggedFavoriteBtn,
    flaggedFavoriteBtn,
    offerContainer,
    noFavorites,
    changeFavoriteStatus,
    filterBtn,
    cookieBanner
}